{
  "name": "chronos-share",
  "private": true,
  "scripts": {
    "gen": "node scripts/gen-manifest.mjs",
    "dev": "npm run gen && npx wrangler dev",
    "deploy": "npm run gen && npx wrangler deploy"
  }
}
